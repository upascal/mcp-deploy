# mcp-deploy Environment Variables

# Encryption key for sensitive data (tokens, secrets) stored in SQLite.
# Auto-generated on first run if not present.
# DO NOT COMMIT THIS KEY TO VERSION CONTROL.
ENCRYPTION_KEY=

# Optional: GitHub personal access token for higher API rate limits
# when resolving MCP metadata from GitHub releases.
# See: https://github.com/settings/tokens
GITHUB_TOKEN=

# Optional: OAuth password for deployed MCPs that use OAuth authentication.
# If set, this will be used as the default OAuth password for new deployments.
OAUTH_PASSWORD=

# Optional: Custom app URL (for OAuth callback URLs)
# Defaults to localhost:3000 in development, auto-detected in Vercel deployments.
APP_URL=
